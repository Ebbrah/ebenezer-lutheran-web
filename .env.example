# Public site URL — production must match your Vercel domain for OG tags & sitemap
NEXT_PUBLIC_SITE_URL=https://ebenezerlutheran.or.tz

# ChMS app (separate Vercel project). Override if your app lives elsewhere.
NEXT_PUBLIC_CHMS_APP_URL=https://app.ebenezerlutheran.or.tz

# Supabase (optional until contact form persistence is needed).
# Create a project at https://supabase.com — same org as ChMS or separate.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
